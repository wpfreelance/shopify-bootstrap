<div class="">
  <header class="sheet-header">
    {% if advanced_layout_page.published_at != blank %}
      <h2 class="title text-center"><a href="{{ advanced_layout_page.url }}">{{ advanced_layout_page.title }}</a></h2>
      {% else %}
      <h2 class="title text-center">{{ advanced_layout_page.title }}</h2>
    {% endif %}
  </header>

  <section class="sheet-content">
  {% if advanced_layout_page.template_suffix == 'split' %}
    {% include 'content_split' with advanced_layout_page.content %}
  {% else %}
    <div class="content-full">
      {{ advanced_layout_page.content }}
    </div>
  {% endif %}
  </section>
</div>
